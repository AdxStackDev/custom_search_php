# custom_search_php
This PHP script as a web API proxy Custom Search. It enables users to perform searches, filter results, and export them in CSV or JSON formats. The API supports both GET and POST methods, allowing for flexible integration with frontend apps or third-party tools.
